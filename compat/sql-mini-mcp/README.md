# sql-mini-mcp

This package has been renamed to
[sql-safe-mcp](https://github.com/proprock/sql-safe-mcp).

Please migrate to:

```bash
uvx sql-safe-mcp
```

or:

```bash
pip install sql-safe-mcp
```

`sql-mini-mcp` is deprecated and receives no further development.
